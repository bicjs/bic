<html>

<head>
	<meta charset="UTF-8">
	<meta name="viewport" content="width=device-width, initial-scale=1, maximum-scale=1">

	<title><%= common.title %> | <%= locals.title %></title>

</head>

<body>

	<div class="background-img"></div>

	<h1><%= common.title %> | <%= locals.title %></h1>

	<p class="flex">
		This is a test.
	</p>

	<ul>
	  <% common.images.forEach(function iterate(src) { %>
	    <li><img src="<%= src %>" /></li>
	  <% }); %>
	</ul>

</body>

</html>
